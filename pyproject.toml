[project]
name = "cp-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cairosvg>=2.7.0",
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "svgwrite>=1.4.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
